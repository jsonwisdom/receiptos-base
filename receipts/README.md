# receipts

Harness receipt artifacts are emitted here at runtime as:

```text
receipts/{run_id}.json
```

Do not commit claimed `LOAD_VERIFIED` receipts unless they satisfy `LOAD_VERIFICATION_WITNESS_V0_1`.
