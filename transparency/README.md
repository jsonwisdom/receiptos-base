# transparency

Harness transparency artifacts are emitted here at runtime as:

```text
transparency/log/{log_id}.json
transparency/index/chain_head.json
```

Transparency continuity is required before any `LOAD_VERIFIED` promotion.
