name: CRP Compliance

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  compliance:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show repository
        run: |
          pwd
          ls -la

      - name: Placeholder
        run: |
          echo "CRP compliance workflow initialized."
          echo "Rust build, canonicalization, fixtures, and determinism checks will be added next."
