name: replay-witness

on:
  pull_request:
    branches: [ main ]

jobs:
  witness:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Replay witness placeholder
        run: |
          echo "ReceiptOS Base: replay witness OK"
